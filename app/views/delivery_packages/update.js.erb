// function updateAmount(amountHTML) {
//   const amount = document.getElementById('package-amount-<%= @delivery_package.delivery.id %>');
//   amount.innerHTML = amountHTML;
// }

// function closeModal() {
//   const closeButton = document.querySelector('#modal-<%= @delivery_package.delivery.id %> .close');
//   closeButton.click();
// }


// updateAmount("<%= j render 'deliveries/delivery_details', delivery: @delivery_package.delivery %>");
// closeModal();
// TODO : See if relevant.
